贪心！

未配对奶牛重量=总重量-配对奶牛重量。 

先按x排序。

T=2：

不存在1 2 1 2的配对。

因为可以转化为1 1 2 2。

如果$i,j$可以配对，则$(i,j)$间不再有被选的，且$x_{i+1}-x_{i-1}$和$x_{j+1}-x_{j-1}$都大于K，$x_j-x_i\le K$。

预处理出所有$x_{i+1}-x_{i-1}>K$的 i

在所有的i中，对于$(i,j)$如果$x_j-x_i\le K$则连上一条权值为$y_i+y_j$的边，跑最小权最大匹配。

T=1：

把$x_i-x_{i-1}>K$的分开。