# SAM 学习笔记

[~~山姆自动机~~](http://www.sinosam.com/)

一个串的 SAM 是能且仅能接受它的全部后缀的自动机（它有一些终止结点）。

- $\rm endpos\it s$(end position)：是子串 s 在串中出现（可以重叠）的所有的结束位置的集合。

- $\rm endpos等价类$：每个等价类有一个最长的串，等价类中其他的串都是它的后缀，记其长度为 $\rm len$。在 SAM 自动机中，一个结点代表一个等价类。
- `link