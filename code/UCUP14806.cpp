#include<bits/stdc++.h>
int main(){
    int T;scanf("%d",&T);
    while(T--){
        int n,q;scanf("%d%d",&n,&q);
        for(int i=1;i<=q;i++){
            char op[3];scanf("%s",op);
            int u,v,w;
            if(op[0]=='+') scanf("%d%d%d",&u,&v,&w)
        }
    }
}